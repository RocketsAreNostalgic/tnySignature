<?xml version="1.0"?>
<ruleset name="WordPress Plugin Coding Standards (DEPRECATED)">
    <description>DEPRECATED: Please use .phpcs.xml instead. This file is kept for backward compatibility.</description>

    <!-- What to scan -->
    <file>.</file>
    <exclude-pattern>/vendor/</exclude-pattern>
    <exclude-pattern>/node_modules/</exclude-pattern>

    <!-- How to scan -->
    <arg value="sp"/> <!-- Show sniff and progress -->
    <arg name="basepath" value="./"/><!-- Strip the file paths down to the relevant bit -->
    <arg name="colors"/>
    <arg name="extensions" value="php"/>
    <arg name="parallel" value="4"/><!-- Reduced parallel processing -->

    <!-- Rules: WordPress Coding Standards -->
    <config name="minimum_supported_wp_version" value="4.7"/>

    <!-- Include the WordPress ruleset with some exclusions -->
    <rule ref="WordPress">
        <!-- Exclude problematic or unnecessary rules -->
        <exclude name="WordPress.Files.FileName.NotHyphenatedLowercase" />
        <exclude name="WordPress.Files.FileName.InvalidClassFileName" />
        <exclude name="Generic.Commenting.DocComment.MissingShort" />
        <exclude name="WordPress.PHP.YodaConditions.NotYoda" />

        <!-- Exclude sniffs that have issues with PHP 8.1+ -->
        <exclude name="WordPress.NamingConventions.PrefixAllGlobals" />
        <exclude name="WordPress.WP.I18n" />

        <!-- Exclude deprecated sniffs -->
        <exclude name="Generic.Functions.CallTimePassByReference" />
        <exclude name="Squiz.WhiteSpace.LanguageConstructSpacing" />
    </rule>

    <!-- Text domain rule disabled due to PHP 8.1+ compatibility issues -->
    <!--
    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array">
                <element value="ran-tnygooglekey"/>
            </property>
        </properties>
    </rule>
    -->

    <!-- Prefix rules - disabled due to compatibility issues with PHP 8.1+ -->
    <!--
    <rule ref="WordPress.NamingConventions.PrefixAllGlobals">
        <properties>
            <property name="prefixes" type="array">
                <element value="tnygooglekey"/>
                <element value="RAN\TnyGoogleKey"/>
            </property>
        </properties>
    </rule>
    -->
</ruleset>
